/**
 * this file is part of bundesblock-voting
 *
 * it is subject to the terms and conditions defined in
 * the 'LICENSE' file, which is part of the repository.
 *
 * @author Heiko Burkhardt
 * @copyright 2018 by Slock.it GmbH
 */


export namespace Utils {

    // export const propEquals: any = (name: any, value: any): any => object => object[name] === value;

    // export const propUnequal: any = (name: any, value: any) => object => object[name] !== value;

    // export const wrap = val => Array.isArray(val) ? val : [val];

    // export const flatten = ast => {
    //     const children: any = wrap(ast.body || ast.expression || ast.left || ast.right || ast.literal || []);
    //     return [ast].concat(...children.map(flatten));
    // }

}